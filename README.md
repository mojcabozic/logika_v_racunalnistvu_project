# logika_v_racunalnistvu_project
Project for course Logika v računalništvu (UL FMF). The instructions for the project are located in 
`project.pdf`.

SETUP instructions: download the standard library from https://github.com/agda/agda-stdlib and rename the folder to 
`standard-library`. This folder should be in `./standard-library`.

